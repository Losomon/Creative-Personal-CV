# Local scaffolding plan (10 individual repos)

Each folder will be a standalone project that can be pushed as its own GitHub repo.

Structure per project:
- `frontend/` (React + Vite)
- `backend/` (Node + Express + Prisma + PostgreSQL)
- `README.md` (copied from template + project-specific notes)

MVP starter endpoints to include:
- `GET /health`
- Auth endpoints only when needed for the project (RBAC/multi-tenant)

Note: This scaffolding is generated as templates/placeholders—no real third-party keys required.

